#工程端口号
server:
  port: 5001

spring:
  application:
    name: hystrix-dashboard #工程名，后面各个服务间调用接口要用到
  rabbitmq:
    host: 14.23.124.140
    port: 40071
    username: guest
    password: guest
#eureka注册中心地址
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/ #eureka服务器所在的地址
